#ifndef __MAIN_UI_HPP__
#define __MAIN_UI_HPP__

/**
 * Start the user interface.
 * It is a command line interace that permits to start threads/tasks/actions/send commands.
 */
void runUI();

#endif

